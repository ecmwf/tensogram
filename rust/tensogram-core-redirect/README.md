# tensogram-core

This crate has been renamed to [`tensogram`](https://crates.io/crates/tensogram).

This package re-exports everything from `tensogram` for backwards compatibility.
Update your dependency to use `tensogram` directly:

```toml
[dependencies]
tensogram = "0.15"
```
